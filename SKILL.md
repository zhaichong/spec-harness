---
name: spec-harness
description: 为功能开发、跨文件修改、缺陷修复、数据取数、批量操作、迁移及其他需要范围控制或验证证据的工程任务执行风险自适应的 Spec + Harness 流程。根据复杂度和风险选择 Fast、Standard 或 Strict；对小型低风险修改保持轻量，对需求含糊、高风险、不可逆或具有外部副作用的操作设置人工确认卡点。
---

# Spec + Harness 风险自适应开发

用 Spec 定义“做什么”，用 Harness 约束“如何安全地完成和验证”。目标是以与风险相称的流程产出可审查、可验证的待交付结果。

## 指令优先级

按以下顺序执行；发生冲突时采用更高优先级：

1. 系统与安全规则
2. 用户明确要求和授权边界
3. 项目 `AGENTS.md`、贡献规范、现有工作流
4. 本 Skill 的默认流程

不要让本 Skill 的目录、分支或卡点约定覆盖项目事实。用户给出的清晰实施请求通常已包含实施授权，不要求用户重复回复一次“OK”。

## 核心纪律

- 先读后写：先检查项目规则、目标代码、同类实现和工作区状态。
- 事实与假设分开：不编造技术栈、接口、目录或业务规则。
- 保护用户工作：保留未提交改动，不擅自切分支、覆盖文件或扩大范围。
- 失败如实报告：记录通过、失败、跳过及原因，不伪造验证结果。
- 权限最小化：未经明确授权，不合并、发布或执行影响共享状态、外部系统、第三方人员的操作。
- 终点为待交付：实现和验证完成后交给人 Review；除非用户明确要求，否则不自动上线。

## 选择流程档位

先根据已知事实选择最低但足够安全的档位。拿不准时选择较高一档，并简短说明原因。

| 档位 | 典型条件 | 默认产物 | 人工卡点 |
|---|---|---|---|
| Fast | 范围清晰、局部、可逆、低风险，如小修复、文案、测试补充 | 聊天内范围说明 + 验证结果 | 通常无 |
| Standard | 新功能、跨文件修改、公共接口变化或存在一般回归风险 | `spec.md` + `tasks.md` + `harness-check.md`；复杂时加 `plan.md` | 仅在实质歧义或风险变化时 |
| Strict | 批量写入、迁移、权限/安全/合规、不可逆变更、生产操作或重大架构变更 | 完整目录与全过程留痕 | 实施前和外部执行前必须确认 |

以下任一项通常提升档位：数据删除或批量写入、权限边界、敏感数据、公共 API、跨端兼容、迁移、难以回滚、生产资源、显著成本、第三方通知。

## 产物约定

Standard 或 Strict 在项目根或用户指定工作区创建：

```text
.spec/<YYYY-MM-DD>-<需求简述>/
  spec.md
  plan.md                 # Standard 可选，Strict 必需
  tasks.md
  check_reports/
    harness-check.md
  session/
    log.md                # Standard 可选，Strict 必需
```

使用 `templates/` 中对应模板。不要为了填模板制造无价值文字；不适用项写“不适用”并给出一句原因。Fast 模式默认不创建文件。

状态的唯一来源是 `tasks.md`；`spec.md` 只记录需求及确认信息，检查报告只记录证据，session log 只记录关键事件，避免重复维护完成状态。

## 工作流

### 1. 探测与定档

只读取与任务有关的上下文：

- 项目 `AGENTS.md`、README、贡献规范和 `.harness/` 中相关约束
- 包管理、构建、lint、测试配置
- 目标代码及现有同类实现
- Git 分支、worktree 和未提交改动

优先使用项目提供的结构化代码索引或专用工具；文字内容再用文件搜索。输出一段简短判断：已知事实、关键未知项、选择的档位及理由。

### 2. 对焦需求

至少明确：

- 可观察行为与用户价值
- 角色、触发条件、主路径和边界路径
- 数据读写、接口或外部系统
- 验收标准
- 明确不做的范围
- 回滚或恢复要求

给验收标准分配稳定编号：`AC-01`、`AC-02`。描述必须可观察、可判定，避免“体验良好”“正常工作”等模糊表述。

#### 何时暂停确认

仅在以下情况暂停并请求用户决策：

- 不同理解会导致明显不同的产品行为或实现范围；
- 操作高风险、不可逆，或会产生外部副作用；
- 需要用户提供新权限、凭据、业务取舍或合规判断；
- 实施中发现范围或风险发生实质变化。

低风险实现细节可依据仓库事实合理决定，并在交付时说明。Strict 模式在方案完成后必须展示关键风险、回滚方式和执行范围，获得确认后才实施。

### 3. 方案与任务

Standard 在涉及调用链、公共接口或多种可行方案时写 `plan.md`；Strict 始终写。

方案至少说明：

- 影响文件、模块、数据流和接口
- 关键选择及原因；只有存在真实取舍时才记录替代方案
- 权限、数据、兼容、安全和回滚风险
- 验证策略及回归关注点
- 明确的范围外事项

将工作拆成可验证任务 `T-01`、`T-02`，每项引用相关验收标准。例如：`T-02 → AC-01, AC-03`。`tasks.md` 是进度状态的唯一来源。

### 4. 安全准备

不要默认创建或切换分支。先检查当前上下文：

- 用户已在专用分支或 worktree：沿用。
- 存在未提交改动：避免覆盖和混入；无法隔离时说明风险。
- 项目明确要求分支：按项目命名规则创建。
- 无项目规则且新分支确有价值：只在不会扰动用户工作时创建，并使用安全、可移植的 slug。

Strict 数据操作还必须检查：

- dry-run 或预览能力
- 幂等性、重复执行与断点续跑
- 分页、数据量、并发、速率限制
- 超时、重试、部分失败和补偿
- 备份、恢复和回滚验证
- 操作前后对账、审计与敏感数据最小化

无法提供充分缓解措施时停止实施并请求决策。

### 5. 实施与变更控制

- 按任务逐项实施，完成定义满足后再勾选。
- 匹配周围代码的命名、抽象和注释密度。
- 不做 Spec 之外的顺手优化。
- 新发现只影响低风险实现细节时，更新 plan/tasks 后继续。
- 新发现改变产品行为、验收范围、外部副作用或风险等级时，更新 Spec 并重新确认。
- 被阻塞、取消或部分完成时，保留真实状态，不将其标为待交付完成。

### 6. 风险导向验证

先运行与改动直接相关的最小检查，再按影响范围扩大。不要机械运行昂贵的全量命令，也不要因为成本高就跳过必要检查。

基础检查包括适用的 lint、format、typecheck、unit/integration test、build 和手工验证。再按变更类型补充：

- UI：真实浏览器、响应式、键盘操作、错误/空/加载状态、视觉回归
- API：契约、鉴权、输入边界、错误码、兼容性
- 数据：dry-run、样本核对、总量对账、重复执行、恢复演练
- 并发/异步：竞态、重复消息、超时、重试和顺序
- 性能：关键路径基线、数据规模和资源上限
- 安全：越权、敏感信息、密钥、危险默认值和日志泄露

在 `harness-check.md` 中按 `AC-ID` 记录结果、证据及未覆盖风险。自审是一种检查视角，不等同于独立 Review；高风险任务应明确要求人工或独立复查。

### 7. 待交付

只有以下条件满足时才标记待交付：

- 所有必须任务完成，或未完成项已明确列出；
- 每个验收标准都有 pass、fail、partial 或 skipped 结论及证据；
- 风险、回滚和验证缺口已如实说明；
- 未发生未经授权的外部副作用。

交付内容保持简洁：改动摘要、AC 对照、关键文件、验证结果、已知风险和下一步。未经明确授权，不执行 merge、push、PR 更新、部署、数据库写入、批处理、消息发送、云资源修改或包发布。

## 模板导航

- `templates/spec.md`：Standard / Strict 的需求与 AC
- `templates/plan.md`：复杂 Standard / Strict 的方案和风险
- `templates/tasks.md`：唯一进度面板及 AC 追踪
- `templates/harness-check.md`：命令、证据和验收结论
- `templates/session-log.md`：Strict 或长任务的关键事件记录
