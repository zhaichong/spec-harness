---
name: spec-harness
description: 用最小必要设计、现有代码复用和定向验证完成功能、缺陷修复与持续需求变更。普通开发直接实施，复杂变化按需保留简短 Spec。适用于前端、Java 和其他技术栈；不用于纯咨询、只读审查或范围已清的机械小改。
---

# Spec + Harness

以最小必要设计、符合项目习惯的实现和真实验证完成需求。需求变化时增量更新，终点是待交付。

## 优先级

遵循系统规则、用户要求、项目 `AGENTS.md` 和既有工作流；本 Skill 是默认方法，不扩大或收回用户授权。

- 用户明确要求实施且目标、边界和验收清楚时，完成必要设计后继续编码，不为 Spec 升版重复索要确认。仅讨论、审查或要求先出方案时，按该范围工作。
- 未说明目标或预期结果且无法从上下文确定时，先询问最关键的缺失信息，不自行设定目标后实施。无需用户一次写完整规格；目标明确后继续查明实现细节。
- 仓库能查明的问题自行查明；命名、私有函数拆分等实现细节沿用项目约定。只有无法查明且影响业务结果、兼容性或授权范围的问题才询问；继续处理不依赖答复的部分。
- 保留用户未提交修改，在当前内容上合并；只有真实冲突或无法辨明意图时暂停相关改动。不编造接口、业务规则、授权或验证结果。

## 定档

普通开发默认 Fast。先查调用方、共享行为和数据影响；只有需要留存关键设计或风险判断时才选 Spec，不以文件数量定档。不确定影响时先定向探测，业务目标不清则先询问。

| 档位 | 何时使用 | 必要工作 |
|---|---|---|
| Fast | 目标清楚的普通功能和缺陷修复；沿用已有契约、数据模型及权限边界，本地修改和隔离验证 | 简短设计后实施并验证；聊天说明结果，不建流程文件、不运行文档校验 |
| Spec | 跨模块职责取舍、共享契约或数据模型变化、关键公共行为、影响仍难界定或高风险变化 | 默认只留一份简短设计记录，复用已有项目文档；按 [Spec 流程](references/spec-workflow.md) 执行 |

改变权限或敏感数据边界、外部副作用、不可逆、生产或批量行为时增加针对性复查。独立复查在高风险部分交付或实际操作前完成，可先做不依赖未决结论的本地实现和隔离测试。普通业务写库、沿用现有登录机制不自动触发。实际共享/外部操作另核对授权，已有授权范围不变时沿用。

## 实施与质量

1. **定位**：读取相关规则、Git 状态、同类实现及构建测试入口。确认调用链、职责归属、复用点和受影响调用方后再写；定位清楚即停止扩展检索。
2. **最小设计**：沿用项目分层与契约，业务规则集中在已有业务边界内。优先现有代码、标准库和已安装依赖；新依赖、抽象层或通用框架须有当前需求中的具体理由。不为凑层级或追求少行数牺牲可读性。
3. **实施**：先完成一条可验证的业务闭环，再补分支；保持中间改动可检查。修缺陷先建立失败测试或可复现步骤，定位根因后修改。测试面向可观察结果，避免重复实现逻辑。
4. **审查实际 diff**：检查职责混杂、重复规则、遗漏调用方、错误被吞、兼容性与必要边界；去掉本次引入的无关改动和调试残留，不顺带重构相邻模块。
5. **验证并交付**：执行下述定向检查；说明结果、证据及尚未覆盖的问题，不把未执行写成通过。

## 验证选择

- 项目规定的检查必须完成。其余按影响选择静态/类型检查、编译、目标测试、集成或浏览器验证，覆盖改变的行为和关键失败路径。
- 不默认全量构建，也不一概跳过构建。依赖、资源引用、生成代码、编译链变化，或较轻检查无法覆盖风险时，运行最小相关构建；先识别脚本中发布、删除或共享写入等副作用。
- 检查失败先区分本次回归、既有失败和环境问题，定位后再扩大验证。检查通过后，只有相关代码、依赖、配置、环境变化或新风险出现才重跑。
- 低风险文案、样式、机械修改优先已有检查，不为流程新增低价值测试。无法验证时说明缺口，保留待处理状态。

## 持续变更与上下文

用户追加或修改需求时读取 [增量变更规则](references/change-management.md)，定位受影响的验收点、代码和验证；只更新相关部分。未受影响且仍适用的设计与证据可复用，无法确认适用性时重新验证。

- 每次只读适用的领域规则。主流程由本 Skill 负责，其他 Skill 只补当前所需能力，不重复规划、产出报告或执行同一检查。模板与格式校验仅在明确需要完整追踪记录时启用。结构查询优先可用代码索引；不可用时定向搜索，已读且未变化的内容不重复加载。
- 独立的只读查询可以批量执行；依赖操作与写入按顺序进行，不为普通任务默认增加多 Agent。
- 阶段更新只说明新发现、变更和阻塞；交付说明完成内容、验证和遗留项，不全文重述 Spec、代码或日志。
- 同一任务复用当前文件，不创建重复 Spec 目录；恢复时核对当前代码、Git 和最新需求，淘汰失效假设。

## 按需参考

- 页面、交互、前端接口或构建：[前端规则](references/frontend.md)。
- Java 服务、API 或数据访问：[Java 规则](references/backend-java.md)。
- 双方共享 API、事件或字段语义：[契约规则](references/fullstack-contract.md)。
- 简短 Spec、针对性复查及何时需要完整记录：[Spec 流程](references/spec-workflow.md)。其他技术栈按主流程和项目规范处理。
