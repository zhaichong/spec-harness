# Harness Check：{{标题}}

> 时间：{{YYYY-MM-DD HH:mm}}
> 工作上下文：{{branch/worktree 或“无 git”}}
> 验证基线：{{提交及本次未提交改动摘要；必要的依赖、配置、测试环境}}

## 执行记录

| 检查 | 命令/步骤 | 结果 | 关键证据或原因 |
|---|---|---|---|
| 静态检查 | | pass/fail/skipped | |
| 自动化测试 | | pass/fail/skipped | |
| 编译/构建 | | pass/fail/skipped | 按依赖、资源、生成代码、编译链及项目要求选择 |
| 前端验证 | 浏览器、交互、状态或前端构建 | pass/fail/skipped | |
| Java 后端验证 | 单元/集成测试、接口或服务端构建 | pass/fail/skipped | |
| 契约验证 | 字段、错误、权限与兼容性 | pass/fail/skipped | |
| 端到端验证 | 主路径及关键失败路径 | pass/fail/skipped | |
| 专项验证 | | pass/fail/skipped | |
| 手工验证 | | pass/fail/skipped | |

只保留适用的检查行；应做但未执行的检查标 `skipped` 并说明原因。失败保留定位所需关键输出。已通过且仍适用的结果不重复执行。

## 验收追踪

Spec 中每个 AC 必须各占一行并为 `pass`；同一 AC 不得重复。证据必须具体且可追溯，不能留空、保留模板占位符或仅写“正常/通过/已验证”，且必须包含任务目录内的非空材料引用，例如 `文件：evidence/AC-01-test.txt`。缺行或 `fail`、`partial`、`skipped` 不能得出“待交付”。按需复制下行。

多个 AC 可引用同一份输出，各自说明其验收结果。复用旧证据时注明原基线与仍适用的依据；需求、代码、依赖、配置或环境变化影响该结果时改为 `partial` 并重新验证，不将旧日志标为本次运行。

| AC | 结果 | 证据类型 | 证据 | 未覆盖风险 |
|---|---|---|---|---|
| AC-01 | pass | 自动化 | 命令/观察摘要；文件：evidence/AC-01-test.txt | |

## 风险导向检查

仅保留适用项：

- UI：浏览器、响应式、键盘、空/错/加载状态
- API：契约、鉴权、边界、错误码、兼容性
- 数据：dry-run、幂等、分页、重试、对账、恢复
- 并发：竞态、重复、超时、顺序
- 性能：基线、数据规模、资源上限
- 安全：越权、敏感信息、密钥、危险默认值
- 部分写入或外部调用失败时不留半成品
- 主操作成功、后续状态或清理失败时对外结果仍收敛
- 重试或重复操作不产生第二份副作用
- 中断后可恢复，且不重做已执行的外部操作

## 自审与独立复查

- 实际 diff 的发现与处理：{{职责归属、重复规则、调用方兼容、异常与边界、无关改动}}
- 必要复查记录：不适用 / `session/independent-review.md`；已有结论只引用，不复制。

## 验证结论

- 结论：验证通过 / 验证未通过
- 交付状态：以 `tasks.md` 为准
- 阻塞或遗留项：无 / …
