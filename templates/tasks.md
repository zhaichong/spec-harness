# Tasks：{{标题}}

> 本文件是进度状态的唯一来源。状态：未开始 / 进行中 / 已阻塞 / 待交付
> 每项任务标记为 `[required]` 或 `[optional]`；每个当前 AC 必须至少由一项 `[required]` 任务引用，关联任一当前 AC 的任务必须是 `[required]`。`[optional]` 不得用来绕过 AC、风险缓解或交付门禁。
> 需求变更时保留编号，只重新打开受影响任务；删除或替代项更新当前映射。存在待验证变更时撤销待交付状态。
> 每个任务按下方格式写成一行，编号唯一；required 必须引用当前 AC，多个编号用逗号分隔；optional 无关联 AC 时写 `→ 无`。缺状态、分类或映射会报错，不会被跳过。代码块只作示例，实际任务写在正文。

## 任务

- [ ] **T-01** [required] {{任务名}} → AC-01
  - 文件/模块：
  - 工作内容：
  - 完成定义：

## 当前状态

- 状态：未开始
- 当前任务：T-01
- 阻塞项：无
- 未完成项：T-01
