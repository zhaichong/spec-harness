# Spec：{{标题}}

> 目录：`.spec/{{日期}}-{{需求简述}}/`
> 流程档位：Spec
> Spec 版本：v{{n}}（目标、范围、事实、假设、AC 或风险变化时递增）
> 原始需求：{{用户原话或可定位会话引用}}
> 审核状态：{{fresh review / 未独立（原因） / 人工复查 / 阻塞}}
> 作者确认：待确认 / Spec v{{n}}，{{作者原话或会话引用}}；加码再记录执行确认

## 目标与价值

{{用可观察行为描述要做什么、为谁解决什么问题}}

## 变更面

每项必须写成单独的「是」或「否」，不要保留「是 / 否」。

- 前端：否
- Java 后端：否
- 数据库：否
- 外部系统：否
- 数据写入/删除：否
- 权限/敏感数据：否
- 外部副作用：否
- 不可逆：否
- 生产或批量：否

## 场景与规则

- 触发角色：
- 触发条件：
- 主路径：
- 异常与边界：

## 数据、接口与外部副作用

| 类型 | 内容 | 权限/敏感性 |
|---|---|---|
| 读取 | 无 / … | |
| 写入 | 无 / … | |
| 接口/事件 | 无 / … | |
| 外部系统 | 无 / … | |

## 接口契约

仅在任务新增、修改或依赖前后端契约时填写；不适用写“不适用”。

| 接口/事件 | 请求 | 响应 | 错误与权限 | 兼容要求 |
|---|---|---|---|---|
| | | | | |

## 验收标准

风险只能写 `低`、`中` 或 `高` 之一；证据类型只能写 `自动化`、`可复现命令`、`浏览器` 或 `人工` 之一。不要把备选项留在同一行。

- [ ] **AC-01** [风险：低] [证据类型：自动化] {{可观察、可判定的结果}}
- [ ] **AC-02** [风险：中] [证据类型：可复现命令] {{边界或失败行为}}

## 关键风险映射

数据写入/删除、权限/敏感数据、外部副作用、不可逆、生产或批量为「是」时各写一项映射，括号内只写该变更面，并指向高风险 AC；没有则写「无」。普通写库标「数据写入/删除」，不要因此标「生产或批量」。

- KR-01 [数据写入] → AC-02

## 非目标

- {{明确不做的内容}}

## 已知约束、事实与假设

| 陈述 | 类型 | 来源/依据 | 状态 |
|---|---|---|---|
| | 事实 / 假设 / 约束 | 用户原话、`文件路径:符号/字段`、`路径/URL#章节`，或命令/查询记录（时间、范围、关键输出及报告路径）；假设写待验证原因 | 已确认 / 待验证 |

## 审核发现

- 自审：无阻塞 / 待作者答复
- Q：无；有问题时写「Q-01：问题与影响 → 作者答复 → 已更新至 Spec vN」。
