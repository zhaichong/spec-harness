# Spec：{{标题}}

仅用于已启用完整追踪的任务。普通简短 Spec 不复制本模板，按 references/spec-workflow.md 记录必要内容即可。单值元数据只保留一处，更新原字段，历史变化写入说明。

> 目录：`.spec/{{日期}}-{{需求简述}}/`
> 流程档位：Spec
> Spec 版本：v{{n}}
> 原始需求：{{用户原话或可定位会话引用}}
> 审核状态：{{自审通过 / 独立复查通过 / 待澄清 / 阻塞；选择一个值}}
> 实施授权：{{用户要求实施的原话或会话引用，以及涵盖的范围；未获授权写待确认}}

版本字段只保留版本号；业务、契约、范围、AC 或风险实质变化才升版。审核状态只填一个明确状态，解释写在「审核发现」；待澄清或阻塞可以保存草案，但不能交付。以下非适用章节可省略，保留元数据、变更面、验收与审核结论。不重复填写其他文件已有内容。

## 目标与价值

{{用可观察行为描述要做什么、为谁解决什么问题}}

## 变更面

每项必须写成单独的「是」或「否」，不要保留「是 / 否」。

- 前端：否
- Java 后端：否
- 数据库：否
- 外部系统：否
- 数据写入/删除：否
- 权限/敏感数据：否
- 外部副作用：否
- 不可逆：否
- 生产或批量：否

## 场景与规则

- 触发角色：
- 触发条件：
- 主路径：
- 异常与边界：{{无写入且未加码可略；否则写提交点、重复执行结果、禁止结果}}

## 数据、接口与外部副作用

| 类型 | 内容 | 权限/敏感性 |
|---|---|---|
| 读取 | 无 / … | |
| 写入 | 无 / … | |
| 接口/事件 | 无 / … | |
| 外部系统 | 无 / … | |

## 接口契约

仅在任务新增、修改或依赖前后端契约时填写；不适用写“不适用”。

| 接口/事件 | 请求 | 响应 | 错误与权限 | 兼容要求 |
|---|---|---|---|---|
| | | | | |

## 验收标准

风险只能写 `低`、`中` 或 `高` 之一；证据类型只能写 `自动化`、`可复现命令`、`浏览器` 或 `人工` 之一。不要把备选项留在同一行。

每个 AC 按下方格式写成独立一行，编号唯一、字段完整且写明验收结果。实际记录放在正文，代码块仅作示例；缺字段或格式错误会报告，不能因此跳过该验收项。

- [ ] **AC-01** [风险：低] [证据类型：自动化] {{可观察、可判定的结果}}
- [ ] **AC-02** [风险：中] [证据类型：可复现命令] {{边界或失败行为}}

## 关键风险映射

数据写入/删除、权限/敏感数据、外部副作用、不可逆、生产或批量为「是」时各写一项映射，括号内只写该变更面，并指向高风险 AC；没有则写「无」。普通写库标「数据写入/删除」，不要因此标「生产或批量」。

- KR-01 [数据写入] → AC-02

## 非目标

- {{明确不做的内容}}

## 已知约束、事实与假设

| 陈述 | 类型 | 来源/依据 | 状态 |
|---|---|---|---|
| | 事实 / 假设 / 约束 | 用户原话、`文件路径:符号/字段`、`路径/URL#章节`，或命令/查询记录（时间、范围、关键输出及报告路径）；假设写待验证原因 | 已确认 / 待验证 |

## 审核发现

- 自审：{{以反方视角核对假设、失败路径和验收，记录具体发现与处理；无发现则简述核对依据}}
- 待澄清：无；仅列无法查明且影响业务结果、兼容性或授权范围的问题，附答复与处理。
- 独立复查：不适用 / `session/independent-review.md`；详细对抗自审文件按需使用。

## 本次需求变更

首次实现可省略。后续仅记录：变化 → 影响的 AC/模块 → 重验范围及证据复用依据。不因升版重复申请相同范围授权。
